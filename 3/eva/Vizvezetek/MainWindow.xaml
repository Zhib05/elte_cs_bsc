<Window x:Class="Vizvezetek.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Vizvezetek"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <DockPanel>
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="Új játék" Command="{Binding NewGameCommand}"/>
            <MenuItem Header="Méret">
                <MenuItem Header="9 x 9" Command="{Binding ChangeSizeCommand}" CommandParameter="9"/>
                <MenuItem Header="12 x 12" Command="{Binding ChangeSizeCommand}" CommandParameter="12"/>
                <MenuItem Header="15 x 15" Command="{Binding ChangeSizeCommand}" CommandParameter="15"/>
            </MenuItem>
        </Menu>

        <ItemsControl ItemsSource="{Binding Fields}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid Rows="{Binding BoardSize}" Columns="{Binding BoardSize}"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>

            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Button Command="{Binding RotateCommand}" Margin="1">
                        <TextBlock Text="{Binding TextRepresentation}" FontWeight="Bold" FontSize="16"/>
                    </Button>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </DockPanel>
</Window>
