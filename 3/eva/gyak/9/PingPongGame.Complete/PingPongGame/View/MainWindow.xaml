<Window x:Class="ELTE.PingPongGame.View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ELTE.PingPongGame.View"
        mc:Ignorable="d"
        Title="Ping-pong játék" Height="450" Width="800">

    <Window.InputBindings>
        <KeyBinding Command="{Binding MovePadCommand}" CommandParameter="Right" Key="Right"/>
        <KeyBinding Command="{Binding MovePadCommand}" CommandParameter="Left" Key="Left" />
        <KeyBinding Command="{Binding StartGameCommand}" Key="Space" />
    </Window.InputBindings>

    <Grid Background="White" Width="{Binding GridSize.Width, Mode=OneTime}" Height="{Binding GridSize.Height, Mode=OneTime}">
        <Grid.Style>
            <Style>
                <Style.Triggers>
                    <DataTrigger Binding="{Binding IsCollision}" Value="True">
                        <DataTrigger.EnterActions>
                            <BeginStoryboard>
                                <Storyboard>
                                    <ColorAnimation Storyboard.TargetProperty="(Grid.Background).(SolidColorBrush.Color)" From="White" To="Red" 
                                        FillBehavior="Stop" AutoReverse="True" Duration="0:0:0.5" />
                                </Storyboard>
                            </BeginStoryboard>
                        </DataTrigger.EnterActions>
                    </DataTrigger>
                </Style.Triggers>
            </Style>
        </Grid.Style>
        <Ellipse
          x:Name="Ball"
          Width="{Binding BallSize.Width, Mode=OneTime}" Height="{Binding BallSize.Height, Mode=OneTime}"
          Margin="{Binding BallStartPosition, Mode=OneWay}"
          HorizontalAlignment="Left" VerticalAlignment="Top"
          Fill="Blue" Stroke="Black"
          LayoutUpdated="OnBallLayoutUpdated" />
        <Rectangle 
            x:Name="Pad"
            Width="{Binding PadSize.Width, Mode=OneTime}" Height="{Binding PadSize.Height, Mode=OneTime}"
            Margin="{Binding PadStartPosition, Mode=OneWay}"
            HorizontalAlignment="Left" VerticalAlignment="Top"
            Fill="Black" Stroke="Black"
            LayoutUpdated="OnPadLayoutUpdated" />
    </Grid>
</Window>
